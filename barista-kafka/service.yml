apiVersion: v1
kind: Service
metadata:
  name: barista-kafka-service-cluster-ip
spec:
  ports:
  - port: 8090
    protocol: TCP
    targetPort: 3000
  selector:
    app: barista-kafka-deployment
  type: ClusterIP